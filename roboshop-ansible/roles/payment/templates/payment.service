[Unit]
Description=Payment Service

[Service]
User=root
WorkingDirectory=/app
Environment=CART_HOST=cart-{{ env }}.bihamlanet.store
Environment=CART_PORT=8080
Environment=USER_HOST=user-{{ env }}.bihamlanet.store
Environment=USER_PORT=8080
Environment=AMQP_HOST=rabbitmq-{{ env }}.bihamlanet.store
Environment=AMQP_USER= {{ secrets.rabbitmq_user }}
Environment=AMQP_PASS={{ secrets.rabbitmq_pass }}

ExecStart=/usr/local/bin/uwsgi --ini payment.ini
ExecStop=/bin/kill -9 $MAINPID
SyslogIdentifier=payment

[Install]
WantedBy=multi-user.target